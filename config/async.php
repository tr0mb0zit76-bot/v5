<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Async feature flags (opt-in)
    |--------------------------------------------------------------------------
    |
    | Все флаги по умолчанию false — поведение CRM остаётся синхронным, пока
    | явно не включите флаг и не запустите queue worker (queue:work / supervisor).
    |
    */

    'outbound_mail' => (bool) env('ASYNC_OUTBOUND_MAIL', false),

    /*
    |--------------------------------------------------------------------------
    | Named queues
    |--------------------------------------------------------------------------
    */

    'queues' => [
        'mail' => env('ASYNC_QUEUE_MAIL', 'mail'),
        'default' => env('ASYNC_QUEUE_DEFAULT', 'default'),
    ],

];
